
using UnityEngine;
//using UnityEngine.SceneManagement;

public class menulevels : MonoBehaviour
{
    
    // Update is called once per frame
    public void LoadLevel(int index)
    {
        //SceneManagement.LoadScene(index);
    }
}
